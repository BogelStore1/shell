#!/usr/bin/env bash
# About module.

about_menu() {
  printf "\n%b┌──────────────────────────────────────────────┐%b\n" "$CYAN" "$RESET"
  printf "%b│%b        %bBOGELSHELL PROTECT%b                   %b│%b\n" "$CYAN" "$RESET" "$BOLD$GREEN" "$RESET" "$CYAN" "$RESET"
  printf "%b├──────────────────────────────────────────────┤%b\n" "$CYAN" "$RESET"
  printf "%b│%b Professional Bash Script Encryptor          %b│%b\n" "$CYAN" "$RESET" "$CYAN" "$RESET"
  printf "%b│%b                                              %b│%b\n" "$CYAN" "$RESET" "$CYAN" "$RESET"
  printf "%b│%b Features                                     %b│%b\n" "$CYAN" "$RESET" "$CYAN" "$RESET"
  printf "%b│%b ✔ Encrypt                                    %b│%b\n" "$CYAN" "$GREEN" "$CYAN" "$RESET"
  printf "%b│%b ✔ Decrypt                                    %b│%b\n" "$CYAN" "$GREEN" "$CYAN" "$RESET"
  printf "%b│%b ✔ Multi Layer Encode                         %b│%b\n" "$CYAN" "$GREEN" "$CYAN" "$RESET"
  printf "%b│%b ✔ Random Variable                            %b│%b\n" "$CYAN" "$GREEN" "$CYAN" "$RESET"
  printf "%b│%b ✔ Random Loader                              %b│%b\n" "$CYAN" "$GREEN" "$CYAN" "$RESET"
  printf "%b│%b ✔ Payload Split                              %b│%b\n" "$CYAN" "$GREEN" "$CYAN" "$RESET"
  printf "%b│%b ✔ Cross Platform                             %b│%b\n" "$CYAN" "$GREEN" "$CYAN" "$RESET"
  printf "%b│%b ✔ Fast Process                               %b│%b\n" "$CYAN" "$GREEN" "$CYAN" "$RESET"
  printf "%b│%b                                              %b│%b\n" "$CYAN" "$RESET" "$CYAN" "$RESET"
  printf "%b│%b Author: Bogel Project                        %b│%b\n" "$CYAN" "$YELLOW" "$CYAN" "$RESET"
  printf "%b└──────────────────────────────────────────────┘%b\n" "$CYAN" "$RESET"
}
